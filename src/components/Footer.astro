---
const quickLinks = [
  { href: '/', label: 'Home' },
  { href: '/pearls', label: 'Pearls of Wisdom' },
  { href: '/events', label: 'Events' },
  { href: '/about', label: 'About Us' },
  { href: '/contact', label: 'Contact' },
];

const services = [
  { href: '/services/consulting', label: 'Business Consulting' },
  { href: '/services/financial', label: 'Financial Planning' },
  { href: '/services/market-analysis', label: 'Market Analysis' },
  { href: '/services/digital', label: 'Digital Transformation' },
];
import { Image } from 'astro:assets';
import logo from '../../public/logo.avif';
import logo2 from '../../public/logo2.avif';
---

<footer class="bg-[#323233] text-white py-12">
  <div class="max-w-7xl mx-auto px-4">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
      <!-- Company Info -->
      <div>
        <div style={{
          padding: '10px', // Padding inside the container
          display: 'flex', // Flexbox for alignment
          alignItems: 'center', // Center images vertically
          justifyContent: 'center', // Center images horizontally
      
      }}>
      <Image src={logo} alt="Logo Part 1" style={{ width: '70px', height: 'auto' }} />
      <Image src={logo2} alt="Logo Part 2" style={{ width: '90px', height: 'auto' }} />
  </div>
        <p class="text-primary mb-4 text-center">CONNECTING BUSINESSES, IDEAS AND PEOPLE</p>
        <div class="space-y-2 text-center">
          <p class="text-gray-400">📍 Kuopio, Finland</p>
          <p class="text-gray-400">📞 (358) 400-549283</p>
          <p class="text-gray-400">✉️ jarkko.surakka@me.com</p>
        </div>
      </div>

      <!-- Quick Links -->
      <div>
        <h3 class="text-xl font-bold mb-4 text-primary ">Quick Links</h3>
        <ul class="space-y-2">
          {quickLinks.map(link => (
            <li>
              <a href={link.href} class="text-gray-400 hover:text-white transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Services -->
      <div>
        <h3 class="text-xl font-bold mb-4 text-primary">Services</h3>
        <ul class="space-y-2">
          {services.map(service => (
            <li>
              <a href={service.href} class="text-gray-400 hover:text-white transition-colors">
                {service.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Newsletter -->
      <div>
        <h3 class="text-xl font-bold mb-4 text-primary">Newsletter</h3>
        <p class="text-gray-400 mb-4">Subscribe to our newsletter for the latest business insights.</p>
        <form class="space-y-2">
          <input
            type="email"
            placeholder="Your email"
            class="w-full px-4 py-2 rounded bg-gray-800 text-white border border-gray-700 focus:outline-none focus:border-primary"
          />
          <button
            type="submit"
            class="w-full px-4 py-2 bg-primary text-white rounded hover:bg-secondary transition-colors"
          >
            Subscribe
          </button>
        </form>
      </div>
    </div>

    <div class="mt-8 pt-8 border-t border-gray-800 text-center text-gray-400">
      <p>&copy; {new Date().getFullYear()} SC Business Development. All rights reserved.</p>
    </div>
  </div>
</footer>